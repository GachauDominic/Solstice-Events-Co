# Neon Postgres connection string (pooled, for serverless functions)
DATABASE_URL="postgresql://neondb_owner:npg_YN8cRWUwd7ty@ep-winter-glade-b1tgjow4-pooler.c-5.eu-central-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

# Public base URL of this app, used to build the webhook callback URL
# handed to the vendor when publishing a print job
APP_BASE_URL="https://checkin.solstice-events.example"

# Vendor's queue publish endpoint + credentials
PRINTER_VENDOR_QUEUE_URL="https://badge-vendor.example/v1/print-queue"

PRINTER_VENDOR_API_KEY="replace-me"

# Shared secret used to verify the HMAC signature on inbound webhook calls
VENDOR_WEBHOOK_SECRET="replace-me"
